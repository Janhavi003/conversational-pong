// =========================
// UI CONFIG
// =========================
export const UI_CONFIG = {
  dialogueHeight: 80,
  padding: 20,
};

// =========================
// BALL CONFIG
// =========================
export const BALL_CONFIG = {
  radius: 8,
  maxSpeed: 5,
  baseVX: 2,
  baseVY: 2.5,
};

// =========================
// PADDLE CONFIG
// =========================
export const PADDLE_CONFIG = {
  width: 100,
  height: 10,
  yOffset: 30,
  easing: 0.18,
};

// =========================
// JUICE CONFIG
// =========================
export const JUICE_CONFIG = {
  hitShake: 6,
  missShake: 10,
  wallShake: 2,
  hitStopHit: 3,
  hitStopMiss: 6,
};
